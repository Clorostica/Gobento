{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "nodemon app/index.js",
    "db:apply": "wrangler d1 execute $CF_D1_DATABASE_ID --file=schema.sql",
    "db:apply:remote": "wrangler d1 execute $CF_D1_DATABASE_ID --file=schema.sql --remote"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-jwt": "^8.5.1",
    "jwks-rsa": "^3.2.0",
    "uploadthing": "^7.7.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "wrangler": "^3.0.0"
  }
}
